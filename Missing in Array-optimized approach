Input: arr[] = [1, 2, 3, 5]
Output: 4
Explanation: All the numbers from 1 to 5 are present except 4.


class Solution {
    int missingNum(int arr[]) {
        // code here
      int XOR1=0;
      int XOR2=0;
      int n=arr.length+1;
      for(int i=0;i<n-1;i++){
          XOR2=XOR2^arr[i];
          XOR1=XOR1^(i+1);
      }
      
          XOR1=XOR1^n;
     
      return XOR1^XOR2;
        
    }
}
